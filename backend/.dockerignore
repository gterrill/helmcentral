.git
.env
.env.local
tmp/
cache/
helmcentral
data/*.sqlite
data/secrets.key
data/routes.json
data/dashboard-layout.json
data/dashboard-pages.json
# Removed feature (ADR 0113). This Dockerfile does `COPY . .`, so
# leftover uploads would stream into the build context. Keep
# ignoring them.
data/sat-charts/
